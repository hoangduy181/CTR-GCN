name: ctrgcn
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - pip
  - pytorch>=1.8.0
  - torchvision>=0.9.0
  - torchaudio
  - pytorch-cuda=11.8  # Change to 12.1 if using CUDA 12.1, or remove for CPU-only
  - numpy>=1.21.0
  - scipy>=1.7.0
  - pip:
    - PyYAML>=5.4.1
    - tqdm>=4.60.0
    - easydict>=1.9
    - tensorboard>=2.8.0
    - scikit-learn>=0.24.0
    - h5py>=3.1.0
    - matplotlib>=3.3.0
    - Pillow>=8.0.0
